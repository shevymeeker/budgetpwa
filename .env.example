# =====================================================
# ExpenseOwl Configuration File
# =====================================================
# This is your main configuration file. Edit the values
# below and save as ".env" (without .example) to use.
# You can safely edit any value here without breaking
# the app - it will use sensible defaults if needed.
# =====================================================

# -----------------------------------------------------
# SERVER SETTINGS
# -----------------------------------------------------

# Port the web server will run on
# Default: 8080
# You can access the app at http://localhost:8080
PORT=8080


# -----------------------------------------------------
# STORAGE SETTINGS
# -----------------------------------------------------

# Storage type: "json" or "postgres"
# - json: Simple file-based storage (recommended for personal use)
# - postgres: Database storage (for advanced users)
# Default: json
STORAGE_TYPE=json

# Storage location/URL
# - For json: Directory path where data files are stored
# - For postgres: Database connection URL
# Default: data (creates a "data" folder in the app directory)
STORAGE_URL=data

# PostgreSQL Settings (only needed if STORAGE_TYPE=postgres)
# Leave empty if using json storage
STORAGE_USER=
STORAGE_PASS=
# SSL mode: disable, require, verify-full, verify-ca
STORAGE_SSL=disable


# -----------------------------------------------------
# DEFAULT USER PREFERENCES
# -----------------------------------------------------
# These are applied ONLY on first run when no data exists
# After that, change them in the app's Settings page

# Default currency code
# Supported: usd, eur, gbp, jpy, cny, krw, inr, rub, brl,
#           zar, aed, aud, cad, chf, hkd, bdt, sgd, thb,
#           try, mxn, php, pln, sek, nzd, dkk, idr, ils,
#           vnd, myr, mad
# Default: usd
DEFAULT_CURRENCY=usd

# Default billing cycle start date (1-31)
# The day of the month when your billing cycle begins
# Default: 1
DEFAULT_START_DATE=1


# -----------------------------------------------------
# ADVANCED SETTINGS
# -----------------------------------------------------

# Maximum CSV file upload size in megabytes
# Default: 10
MAX_UPLOAD_SIZE_MB=10


# =====================================================
# QUICK START GUIDE
# =====================================================
# 1. Copy this file and rename it to just ".env"
# 2. Edit the values above as needed
# 3. Run the app:
#    - Docker: docker-compose up
#    - Direct: go run cmd/expenseowl/main.go
# 4. Open http://localhost:8080 in your browser
# 5. Use the Settings page to customize categories and
#    other preferences
# =====================================================
